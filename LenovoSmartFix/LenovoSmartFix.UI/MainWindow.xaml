<Window
    x:Class="LenovoSmartFix.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Lenovo SmartFix">

    <Grid Background="{StaticResource SurfaceBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="#1A1A1A">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <Border Width="6" Height="24" Background="{StaticResource LenovoRedBrush}" />
                    <TextBlock Text="Lenovo SmartFix"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Grid.Column="1" x:Name="SubtitleText"
                           Text="Pre-Support Resolution"
                           FontSize="11" Foreground="#A0A0A0"
                           HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Main navigation frame -->
        <Frame x:Name="MainFrame" Grid.Row="1" />
    </Grid>
</Window>
